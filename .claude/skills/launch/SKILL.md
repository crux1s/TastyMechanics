---
description: Start the TastyMechanics Streamlit dev server on port 8501
disable-model-invocation: true
allowed-tools: mcp__Claude_Preview__preview_start
---

Start the dev server using the launch.json configuration:

Use the preview_start tool with name "TastyMechanics".

This uses `python3 -m streamlit run tastymechanics.py` as configured in .claude/launch.json — no PATH lookup needed. Report the URL when the server is running.
